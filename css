/* ============================================================
   MY LIBRAS — Global Styles  (Ultra-Realistic Avatar Edition)
   ============================================================ */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap');
*{box-sizing:border-box;margin:0;padding:0}
/* ---------- Layout ---------- */
body{
  background:radial-gradient(ellipse at 20% 30%,#1e3a8a,#0b1a3a 40%,#020617 80%);
  color:#fff;font-family:'Inter',sans-serif;
  display:flex;flex-direction:column;align-items:center;
  min-height:100vh;overflow-x:hidden;padding-bottom:100px;
}
/* ---------- Typography ---------- */
h1{font-size:2.2rem;font-weight:700;margin-top:22px;text-transform:uppercase;letter-spacing:3px;text-shadow:0 2px 12px rgba(0,0,0,.3)}
.gold{background:linear-gradient(135deg,#fff2b0,#d4af37 50%,#8a6a00);-webkit-background-clip:text;-webkit-text-fill-color:transparent}
.sub{font-size:.92rem;color:rgba(255,255,255,.55);margin:4px 0 14px;letter-spacing:.5px}
/* ---------- Avatar Card ---------- */
.avatar-card{
  background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.1);
  border-radius:22px;padding:10px;backdrop-filter:blur(18px);
  box-shadow:0 20px 60px rgba(0,0,0,.45);position:relative;
  transition:box-shadow .6s,border-color .6s;
  max-width:540px;width:100%;
}
.avatar-card.signing{
  box-shadow:0 0 50px rgba(212,175,55,.25),0 20px 60px rgba(0,0,0,.45);
  border-color:rgba(212,175,55,.25);
}
/* subtle idle glow pulse on the card border */
@keyframes cardGlow{
  0%,100%{border-color:rgba(255,255,255,.10)}
  50%    {border-color:rgba(140,170,220,.18)}
}
.avatar-card:not(.signing){animation:cardGlow 4s ease-in-out infinite}
/* canvas scales responsively */
#avatar{
  display:block;border-radius:14px;
  max-width:100% !important;height:auto !important;
}
#signLabel{
  text-align:center;font-size:.8rem;color:rgba(255,255,255,.45);
  margin-top:6px;height:18px;font-weight:600;letter-spacing:1.5px;
  text-transform:uppercase;
}
/* ---------- Text Input ---------- */
.input-row{
  display:flex;gap:8px;margin-top:14px;width:100%;max-width:540px;
}
#textInput{
  flex:1;padding:10px 16px;border-radius:14px;border:1px solid rgba(255,255,255,.12);
  background:rgba(255,255,255,.06);color:#fff;font-family:'Inter',sans-serif;
  font-size:.9rem;outline:none;transition:border-color .3s;
}
#textInput:focus{border-color:rgba(212,175,55,.5)}
#textInput::placeholder{color:rgba(255,255,255,.3)}
#sendBtn{
  padding:10px 18px;border-radius:14px;border:none;
  background:linear-gradient(135deg,#d4af37,#8a6a00);color:#fff;
  font-weight:700;font-size:.85rem;cursor:pointer;letter-spacing:.5px;
  transition:filter .2s;white-space:nowrap;
}
#sendBtn:hover{filter:brightness(1.15)}
/* ---------- Microphone ---------- */
#micBtn{
  background:transparent;border:none;padding:0;cursor:pointer;
  display:flex;align-items:center;justify-content:center;margin-top:14px;
}
.mic-circle{
  width:64px;height:64px;border-radius:50%;
  background:radial-gradient(circle at 30% 30%,#fff2b0,#d4af37 40%,#8a6a00);
  display:flex;align-items:center;justify-content:center;
  box-shadow:inset 0 4px 10px rgba(255,255,255,.35),inset 0 -6px 16px rgba(0,0,0,.35),0 10px 28px rgba(0,0,0,.4);
  position:relative;transition:transform .25s;
}
.mic-circle::after{
  content:"";position:absolute;width:65%;height:65%;border-radius:50%;
  background:radial-gradient(circle at 30% 30%,rgba(255,255,255,.5),transparent 60%);top:10%;left:12%;
}
#micBtn:hover .mic-circle{transform:scale(1.08)}
.mic-circle svg{width:30px;height:30px;fill:#2b1d00}
.mic-circle.listening{animation:pulse 1.5s ease-in-out infinite}
@keyframes pulse{
  0%,100%{box-shadow:inset 0 4px 10px rgba(255,255,255,.35),inset 0 -6px 16px rgba(0,0,0,.35),0 0 0 0 rgba(212,175,55,.4)}
  50%{box-shadow:inset 0 4px 10px rgba(255,255,255,.35),inset 0 -6px 16px rgba(0,0,0,.35),0 0 0 18px rgba(212,175,55,0)}
}
/* ---------- Status & Legenda ---------- */
#status{font-size:.8rem;color:rgba(255,255,255,.4);margin-top:8px;letter-spacing:.3px;transition:color .3s}
#status.active{color:rgba(212,175,55,.8)}
#legenda{
  position:fixed;bottom:30px;left:50%;transform:translateX(-50%);
  background:rgba(0,0,0,.65);padding:14px 28px;border-radius:16px;
  font-size:1.15rem;max-width:80%;text-align:center;line-height:1.5;
  opacity:0;transition:opacity .3s;backdrop-filter:blur(10px);
  white-space:pre-line;pointer-events:none;
}
#legenda.show{opacity:1}
/* ---------- Signs Panel ---------- */
#signsPanel{
  margin-top:18px;max-width:540px;width:100%;
}
#signsToggle{
  background:none;border:1px solid rgba(255,255,255,.1);color:rgba(255,255,255,.5);
  padding:6px 16px;border-radius:12px;cursor:pointer;font-size:.78rem;
  font-family:'Inter',sans-serif;letter-spacing:.5px;transition:.3s;width:100%;
}
#signsToggle:hover{border-color:rgba(212,175,55,.4);color:rgba(255,255,255,.7)}
#signsList{
  display:none;flex-wrap:wrap;gap:6px;margin-top:10px;justify-content:center;
}
#signsList.show{display:flex}
.sign-chip{
  padding:5px 12px;border-radius:10px;font-size:.72rem;cursor:pointer;
  background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.08);
  color:rgba(255,255,255,.55);transition:.2s;font-family:'Inter',sans-serif;
  letter-spacing:.5px;
}
.sign-chip:hover{background:rgba(212,175,55,.15);border-color:rgba(212,175,55,.3);color:#fff}
/* ---------- Help Modal ---------- */
#helpBtn{
  position:fixed;top:16px;left:16px;width:44px;height:44px;border-radius:50%;border:none;
  background:radial-gradient(circle at 30% 30%,#ffd700,#b8860b);color:#1a1a1a;
  font-size:1.3rem;font-weight:bold;cursor:pointer;
  display:flex;align-items:center;justify-content:center;
  box-shadow:0 6px 20px rgba(0,0,0,.35);transition:.3s;z-index:1000;
}
#helpBtn:hover{transform:scale(1.1)}
#helpModal{
  position:fixed;top:0;left:0;width:100%;height:100%;
  background:rgba(0,0,0,.7);display:flex;align-items:center;justify-content:center;
  opacity:0;pointer-events:none;transition:.3s;z-index:999;
}
#helpModal.show{opacity:1;pointer-events:all}
#helpContent{
  background:rgba(10,15,40,.95);padding:28px;border-radius:18px;
  max-width:520px;width:90%;color:#fff;line-height:1.7;text-align:left;
  box-shadow:0 0 30px rgba(0,0,0,.5);
}
#helpContent h2{margin-bottom:14px;font-size:1.2rem}
#helpContent p{font-size:.92rem;color:rgba(255,255,255,.8)}
#closeHelp{
  margin-top:16px;padding:8px 20px;border:none;border-radius:16px;
  background:linear-gradient(135deg,#d4af37,#8a6a00);color:#fff;
  cursor:pointer;font-weight:600;font-size:.85rem;
}
#closeHelp:hover{filter:brightness(1.15)}
/* ---------- Responsive ---------- */
@media (max-width: 560px) {
  h1 { font-size: 1.6rem; letter-spacing: 2px; }
  .sub { font-size: .82rem; }
  .avatar-card { border-radius: 16px; padding: 6px; }
  .input-row { padding: 0 8px; }
}
